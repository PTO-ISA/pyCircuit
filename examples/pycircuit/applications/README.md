# Application examples

Applications combine several pyCircuit features into self-contained runnable
demos. Each application keeps its design, testbench, configuration, optional
emulator, and handwritten host wrapper together. Generated models, profiles,
shared libraries, and traces belong under `.pycircuit_out/`.

These are framework demonstrations, not consumer CPU, NPU, SoC, board, ISA, or
product-reference implementations.
