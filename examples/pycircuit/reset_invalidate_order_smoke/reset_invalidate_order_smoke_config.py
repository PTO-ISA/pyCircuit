from __future__ import annotations

DEFAULT_PARAMS = {
    "width": 8,
}

TB_PRESETS = {
    "smoke": {"timeout": 64, "finish": 2},
    "nightly": {"timeout": 256, "finish": 8},
}

SIM_TIER = "normal"
