# Basic pyCircuit examples

These examples introduce one core authoring concept with minimal supporting
code. Every directory follows the canonical `X.py`, `tb_X.py`, and
`X_config.py` layout and participates in compiler and simulation discovery.
