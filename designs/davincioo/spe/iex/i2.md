# SPE.IEX.I2 — Issue Stage 2

- Source candidate: `DAV-SPE-IEX-I2-0001`
- Hardware hierarchy: **H3**, within H1 `SPE` / H2 `IEX`
- NDF refinement: **L2 microarchitecture**; module-specific L1 behavior links still require review.
- Recommended disposition: **independent H3 leaf**
- Implementation path: `designs/davincioo/spe/iex/i2.py`
- Current design-program execution status: **implemented and gfsim behavior
  verified; H2/H1 integration and stateful PYC/RTL remain pending**.

I2 owns the retained operand/dependency join, execution-transfer, release, and
generated-cancel state. I1 and the read fabric own request/grant state; the
execution sink owns acceptance. This resolves the earlier contained-state
ambiguity without duplicating either owner.

## Inputs

| Name | Payload/type | Meaning | Evidence status |
| --- | --- | --- | --- |
| operand_response | OperandReadResponse | matched I1/read-fabric response | gfsim-verified |
| load_dependency | LoadDependencyResolution | producer hit, miss, or replay | gfsim-verified |
| sink_decision | ExecutionSinkDecision | execute accept or retry | gfsim-verified |
| cancel | IssueCancel | recovery/pipeline cancellation | gfsim-verified |

## Outputs

| Name | Payload/type | Meaning | Evidence status |
| --- | --- | --- | --- |
| execute_request | ExecutePacket | joined operands and execution ownership | gfsim-verified |
| operand_ack | OperandResponseAck | response classification | gfsim-verified |
| dependency_ack | LoadDependencyAck | producer-resolution classification | gfsim-verified |
| sink_decision_ack | ExecutionSinkDecisionAck | matched transfer/retry classification | gfsim-verified |
| release | IssueRelease | ISQ row and dispatch reservation release | gfsim-verified |
| generated_cancel | IssueCancel | load miss/replay cancellation | gfsim-verified |
| cancel_ack | IssueCancelAck | external-cancel classification | gfsim-verified |

These ports use the frozen nominal payloads in
`designs/davincioo/contracts/spe.py`. `IssueAttemptKey` embeds the shared
`IssueIdentity`, and both operand descriptors use the one
`valid_operand_source` invariant. Queue transport is inferred by the compiler.

## Owned or containing state

- scalar owners: `active`, `pending`, `lhs`, `rhs`, `lhs_source`, `rhs_source`,
  `src0_resolved`, `src1_resolved`, `execute_outstanding`, `release_pending`,
  `cancel_pending`, and `cancel_payload`;
- `cancel_tombstones: Array[16, AttemptTombstone]` retains early/generated
  cancellation until the exact producer path is suppressed.

## Required capabilities to verify

- typed compound Queue payloads
- atomic input/state/output rule semantics
- recursive equality and reusable operand invariant lowering
- nominal persistent enum state
- aggregate rule-local capture inside `ac.find` and nested QueueGraph regions

These are requirements, not proof that the current framework is missing each one. First test the current revision; a demonstrated gap becomes a generic framework/primitive issue and regression before a dependent design PR.

## Behavioral acceptance

- Implementation proves this stage/state is contained in the named owner and does not duplicate architectural state.
- All declared/proposed Queue outputs remain stable under backpressure and preserve full identity/generation.
- Stale, duplicate, wrong-flow, and post-recovery responses cause no mutation.
- Generated gfsim C++ executes accepted Queue transfers; the current stateful
  design keeps its explicit PYC/RTL rejection until #22 lands.

gfsim execution is the first implementation gate. PYC/RTL obligations apply to the admitted lowering and remain explicit future work where provisional storage is rejected. Compile-only evidence does not establish behavior.

## Focused implementation evidence

- `pytest -q designs/davincioo/tests/spe/iex/test_i2.py` verifies the exact
  source closure, raw and Frozen ACIR, seven rule blocks, QueueGraph planning,
  generated gfsim C++20 behavior, and the comparison budget. Its matrix covers
  both speculative operands, hit/miss/replay, wrong-producer ABA, execute
  retry/accept/release, early/generated/active/post-accept cancellation,
  independent backpressure on all seven outputs, tombstone capacity/reset, Flow
  isolation, and instance isolation. The existing-tombstone generated-cancel
  path retains its exact row and pending cancel while output capacity is
  unavailable, then publishes exactly once.
- The external baseline contained 284 AST comparison expressions. The in-tree
  source contains 41 after replacing six expanded `IssueAttemptKey` chains,
  producer equality, shared identity, and operand shape checks.

## Open decisions

- Until #25 proves mutually exclusive same-owner effects with output presence,
  the existing-tombstone generated-cancel path conservatively writes the same
  tombstone value back. The behavior matrix verifies its current arbitration,
  backpressure, row preservation, and exactly-once output semantics; #25 may
  remove the redundant write without changing those observations.
- The stateful module remains outside admitted PYC/RTL until provisional Table
  and module-preserving QueueGraph lowering are implemented.

## Contributor closure

- [x] Claim the candidate and identify its parent/containing state owner.
- [x] Resolve disposition; aliases and contained state must not duplicate hardware.
- [ ] Link the relevant NDF L0 intent and L1 behavior to this L2 implementation.
- [x] Freeze port payload fields/widths, producer/consumer, parent seam, state/reset and timing profile.
- [x] Define functional branches, all-or-none effects, contention and cancel/recovery lifecycle.
- [x] Link a minimal failing gate for each actual framework/primitive gap and merge that shared fix first.
- [x] Implement the accepted owner and design-local expected-result tests.
- [x] Prove backpressure, identity/generation, exactly-once effects and isolated instances in gfsim.
- [ ] Integrate into H2/H1 and record admitted PYC/RTL evidence or remaining boundary.

## Source evidence

- `docs/specification/davincioo/ndf-next/interfaces/pycircuit-module-catalog.json:1` — Catalog row: IEX.I2, source srcs/core/spe/iex/i2.py, status planned/deferred.
- `docs/specification/davincioo/ndf-next/scalar/iex.md:45` — Normative NDF owner/refinement clause.
- `docs/specification/davincioo/ndf-next/modules/spe/iex/i2.md:35` — Typed Queue/state contract for existing source draft.

Source paths use the frozen external repository spelling, including legacy `l3/` directories; they are provenance, not the new hierarchy vocabulary. File hashes and the original catalog disposition are in [catalog.json](../../catalog.json). See [architecture](../../ARCHITECTURE.md) for unresolved global decisions.
