# Test goldens

This directory owns generated outputs that tests compare byte-for-byte. Goldens
are verification artifacts, not public examples and not runtime resources.

- `frontend/`: canonical ACPy JSON and ACIR text produced from the source
  fixtures under `tests/python/agentic-circuit/python_frontend/fixtures/`.
