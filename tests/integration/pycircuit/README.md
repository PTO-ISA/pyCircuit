# pyCircuit integration tests

This tree owns cross-frontend, compiler, backend, and runtime fixtures that are
too large for unit tests but are not user-facing examples or public libraries.

- `fixtures/`: generic designs consumed by simulation and nightly gates.
- `applications/`: application-specific runtime regression drivers.
