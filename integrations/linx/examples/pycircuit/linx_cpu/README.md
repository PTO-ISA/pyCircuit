# Linx CPU Program Fixtures

This folder contains program images shared by the supported Linx integration
and platform flows:

- `programs/*.memh` test programs

The retired SystemVerilog testbench targeted ports and internal memories that
the current `linx_cpu_pyc` module does not expose. Git history preserves it;
the active integration gate is
`integrations/linx/flows/tools/run_linx_cpu_pyc_cpp.sh`.
