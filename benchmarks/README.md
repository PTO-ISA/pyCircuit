# Benchmarks

Benchmarks measure performance without defining semantic correctness. Source,
harnesses, and reproducible workload generators are tracked here; generated
binaries, compiler profiles, and reports are written under
`.pycircuit_out/benchmarks/`.
