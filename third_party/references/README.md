# External references

This directory contains pinned upstream source snapshots used as executable
comparison oracles. References are not installed as Agentic Circuit product
code unless a build option explicitly enables them.

Every reference must record its repository, exact commit, selected subtree,
import mode, and license status.
